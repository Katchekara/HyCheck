// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Autoriser ton IP locale en développement
  allowedDevOrigins: ['169.254.205.67', 'localhost'],
};

module.exports = nextConfig;
